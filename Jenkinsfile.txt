pipeline {
    agent any
     stages {  
     stage('Switching on LED') {
               steps { 
                    bat 'python.exe Test1.py' 
                     }
                 }
           }
   }